# TODO

## 2026-08-29 時鐘微調範圍應依時鐘實際大小動態調整（已完成，v2.4.3）

- [x] 修正 `moveClock()` 目前用固定的 `±80px` 限制上下左右鍵可移動的範圍，與時鐘實際尺寸無關，導致：時鐘設定較小時，離區塊邊緣還很遠就已經不能再移動；時鐘設定較大時，卻可以把時鐘移到明顯超出區塊範圍。改為呼叫新的 `clockNudgeLimits()` 依實際量測值取得上下左右各自的極限。
- [x] 可移動範圍改為依目前時鐘實際渲染大小動態計算：`clockNudgeLimits()` 以 `.clock-card` 的 `clientWidth/clientHeight`（＝絕對定位所依據的 padding box）與 `#clock-wrap` 的 `offsetWidth/offsetHeight` 算出四周可用空間；置中模式為 `±(可用空間/2)`，靠上模式為 `0 ~ (H-h)`（往上為 0，不允許超出上緣）。
- [x] 時鐘尺寸或樣式改變時自動收斂：新增 `clampClockOffset()`，掛在 `#clock-wrap` 與 `.clock-card` 兩個既有的 `ResizeObserver` 上，並由 `chooseClockSetting()` 透過 `scheduleClockClamp()`（rAF ＋ 260ms 兩段，涵蓋 `.2s` CSS transition）補觸發；收斂後的值直接寫回 `localStorage`。
- [x] 視窗尺寸改變也重新計算：`.clock-card` 的 `ResizeObserver` 與 `window.resize` 事件都會觸發收斂，與現有 `--clock-max` 自適應量測同一條路徑。
- [x] 實測驗證（1366×768／1366×600／390×844）：小尺寸水平範圍 ±100（舊值 ±80 根本走不到邊）、特大 ±4、數字式特大 ±8／0~25；把小尺寸推到 (100,213) 後切到特大會自動收斂為 (4,117)，量測時鐘四邊與卡片內緣間距為 0.1～0.3px，確認貼齊且未超出。

## 2026-08-29 主畫面工具列精簡（已完成，v2.4.3）

### 移除右下角小時間按鈕

- [x] 移除主畫面右下角工具列的小時間顯示（`#digital-time`／`.toolbar-time`），主畫面已有大時鐘可看目前時間，不需要重複顯示。
- [x] 移除後 `updateScreen()` 中寫入該元素文字的程式碼一併清理，`.toolbar-time` CSS 規則也一併刪除，不留下對已刪除元素的參照或殘留樣式。
- [x] 僅移除這個小時間顯示，工具列其餘的「全螢幕」「設定」按鈕與其功能不變。

## 2026-08-29 時鐘「特大」定義重新設計與全解析度自適應（已完成，v2.4.2）

- [x] 重新定義指針時鐘「特大」：上下緣幾乎與 `.clock-card` 區塊的上下緣切齊（留極小安全邊界避免真的裁切），其餘大／中／小依比例縮小（98%／82%／66%／50%）。改用 `ResizeObserver` 即時量測卡片實際可用寬高（取兩者最小值，寫入 CSS 變數 `--clock-max`），不再依賴手動猜測的 vw/vh 上限，任何解析度、任何視窗比例都能正確適配。
- [x] 重新定義數字時鐘「特大」：時鐘容器改為卡片寬高各 96%（大／中／小依比例為 80%／64%／48%），字體大小以 `min(容器寬度*.34, 容器高度*.85)` 換算並即時反映容器實際像素（`--wrap-px`／`--wrap-h-px`），確保時：分數字大到接近左右邊界又不會裁切。
- [x] 修正因時鐘改為絕對定位後，窄螢幕（≤900px，`.screen` 使用 `auto auto` 列高）下 `.clock-card` 因子元素全為絕對定位、不再貢獻內容高度而幾乎塌陷成 0 的問題：於行動裝置版面明確給予 `.clock-card` 高度（`min(84vw,60vh)`），並將行動裝置的時鐘尺寸格線也依同一套比例調整。
- [x] 行動裝置版面底部固定工具列的預留空間由 13vh 微調為 16vh，降低捲動到頁尾時遮擋內容的機率。
- [x] 已於桌面（1600×900、1366×768）與行動裝置（390×844）三種解析度、指針與數字兩種樣式、置中與靠上兩種對齊、特大～小四種尺寸實測驗證：皆無裁切、無版面塌陷，且特大確實明顯大於前一版。

- [x] 修正數字式時鐘字體大小不會隨後台「大小」設定改變的問題：改用 `ResizeObserver` 即時量測時鐘容器寬度並寫入 CSS 變數 `--wrap-px`，字體大小改以 `calc(var(--wrap-px)*.26)` 換算，確保特大／大／中／小四種設定都會實際反映在數字時鐘上。
- [x] 修正指針時鐘選擇「特大」且「靠上」對齊時下緣被裁切的問題：調降各尺寸的 vh 上限（特大 67→58vh、大 62→53vh、中 54→45vh、小 44→35vh），確保任何對齊模式下時鐘完整落在卡片可視範圍內。
- [x] 主畫面「缺席名單」每張學生卡片（含名單勾選缺席與手動新增缺席）都加上垃圾桶圖示按鈕，按下即可移除該生的缺席狀態（名單學生呼叫既有的 `toggleAbsent`，手動新增者呼叫既有的 `deleteManualAbsent`）。
- [x] 設定頁「學生名單」卡片欄位改為固定四欄，並縮小卡片留白與圖示按鈕尺寸以更緊湊呈現；窄螢幕（≤640px）自動退回兩欄。

## 2026-08-29 指針時鐘置中修正與控制列位置（已完成，v2.4.0）

- [x] 修正指針時鐘選擇「特大」時無法在區塊正中央水平／垂直置中的問題：時鐘改採絕對定位＋`top:50%/left:50%`+`translate(-50%,-50%)`置中，不再依賴 flex 對齊，各尺寸皆已驗證置中誤差為 0。
- [x] 一併修正「靠上」定位模式：改用單一 CSS 自訂屬性（`--anchor-top`/`--anchor-y`）切換錨點，避免先前 `transform` 屬性在兩條規則間出現套用不一致的問題。
- [x] 時鐘位置微調控制列（上下左右／恢復預設）再往左水平移動目前距離的一半，從距左側約 1/18 調整為約 1/36。
- [x] 版號自 v2.3.0 更新為 v2.4.0（`<title>`、設定頁標題、`APP_VERSION`）。

## 2026-08-29 主畫面互動與版面微調（已完成）

- [x] 全站按鈕（`.btn`／`.chip`／`.day-tab`／時鐘樣式選擇按鈕）新增按下時的縮放與變暗回饋，讓使用者確認點擊已生效。
- [x] 設定頁「測試語音」改為即時套用目前語速、音量滑桿數值播放，不需先按「儲存提醒設定」。
- [x] 出缺席「應到／實到／缺席」三欄字體大小統一（同一 clamp 與字型），並改為垂直置中。
- [x] 今日考程日期（例如「8月29日（六）」）字體加大，略大於「今日考程」標題。
- [x] 主畫面「請準備ＯＯ考試」狀態標籤（`phase-soon`）加上緩慢呼吸縮放動畫以提高醒目度。
- [x] 主要考試資訊原本分開的「HH:MM 開始」與「倒數 MM:SS」兩行整併為一行「HH:MM 開始（倒數 MM:SS）」，字體大小統一。

## 修改作業方式

- 後續需求先集中記錄於本檔案，不逐項修改程式或發布。
- 等使用者明確要求「一次修改並 Push」後，再統一實作、測試、Commit 與發布。

## 下一批待修改需求

> v2.3.0 已完成本批介面、考程狀態、語音、時鐘、年級篩選與離線資料清理功能。完整內建掃描 PDF 繁體中文 OCR 引擎仍保留在本節，待評估單一 HTML 內嵌辨識模型的檔案大小與瀏覽器相容性後實作；目前文字型 PDF 仍採純本機解析，不會上傳資料。
>
> 2026-08-29 對照現有程式碼重新檢查本節：「缺席名單輸入欄位」「快速修改考程的科目選單」「時鐘操作介面微調」「出缺席數字輸入樣式」「設定畫面彩色化」「時鐘樣式設定介面」「考前提醒欄位排列」「PDF 解析後的年級篩選與離線資料」「已結束考程未自動隱藏」「考試結束後切換自習模式」等節經逐項核對，程式已實作但先前未勾選，一併補記為完成；僅「純本機 PDF 解析與 OCR」（掃描 PDF OCR 部分）確認仍未實作。

### 缺席名單輸入欄位

- [x] 手動新增或修改缺席者的欄位標籤由「座號（可留空）」改為「座號」。

### 快速修改考程的科目選單

- [x] 展示畫面的「修改考程」彈窗，科目選擇邏輯須與設定頁面的考程編輯一致，避免同一科目被重複排入考程。
- [x] 正在修改的這筆考程原科目維持可選，讓使用者只修改日期或時間時不會被阻擋。
- [x] 已排入其他考程、但尚未考完的科目仍顯示在選單中，文字格式為「英文（已列入考程）」並停用，讓使用者看得到但無法選擇。
- [x] 已經考完的科目仍顯示但停用，文字格式為「英文（已考完）」。
- [x] 已考完科目統一排列在科目清單最後面；可選科目排最前，其次為已列入考程但尚未考完的科目。
- [x] 「已考完」狀態依可校正的程式日期與時間，以及該科考程的結束時間判定，不直接固定使用裝置系統時間。
- [x] 快速修改考程儲存時仍須再次檢查科目重複，避免使用者開啟彈窗後資料已在其他操作中改變。

### 時鐘操作介面微調

- [x] 時鐘位置微調控制列維持靠齊區塊下緣，從目前水平約 `1/9` 的位置再向左移動一半距離，調整為距左側約 `1/18`。
- [x] 校時視窗的「恢復系統時間」改為「與設備時間相同」。
- [x] 校時視窗的「套用」改為「確定」。

### 出缺席數字輸入樣式

- [x] 移除「應到」與「實到」數字框左右兩側自製的 `＋`、`－` 按鈕。
- [x] 保留 `type="number"` 數字輸入框，使用者點選或聚焦後直接使用系統／瀏覽器原生的上下箭頭微調數值。
- [x] 移除加減按鈕後重新調整數字框的寬度、置中與留白，避免畫面留下多餘空間。
- [x] 原有非負整數及「實到不得大於應到」的數值防呆規則維持不變。

### 設定畫面彩色化

- [x] 僅將「設定」畫面改為較有層次的彩色介面，透過分區底色、標題色、頁籤狀態、欄位邊框及功能按鈕顏色提高可讀性。
- [x] 考程設定、學生名單、出缺席、提醒與時間、離線使用可採不同的識別色，但整體色盤必須一致，不使用過度飽和或刺眼的大片亮色。
- [x] 一般操作、主要操作、成功、警告與刪除按鈕使用一致且容易辨識的語意色彩。
- [x] 輸入框、已選科目、已排定科目、停用項目及錯誤訊息須有明確的色彩差異，並維持足夠的文字對比。
- [x] 設定畫面仍以深色系為基礎，彩色主要用於區分資訊層次與操作狀態，避免長時間設定時造成視覺疲勞。
- [x] 主展示畫面的時鐘、目前考試資訊、今日考程及出缺席區域維持現有色彩配置，不受設定畫面彩色化影響。
- [x] 設定畫面的彩色樣式必須同時納入下載的離線使用檔，且不得依賴外部樣式或網路資源。

### 設定頁面版面重新設計

- [x] 重新設計「設定」頁面各分頁（考程設定、學生名單、出缺席、提醒與時間、離線使用）的項目編排與區塊分隔，提升可讀性，不得刪減任何現有項目或功能。
- [x] 以考程設定分頁為例，「新增考程」「上傳 PDF」「全部考程」等現有區塊須有更清楚的分組與間距，避免欄位、按鈕與清單擠在一起造成閱讀困難。
- [x] 檢視每個分頁內欄位的排列順序是否符合操作邏輯（例如先選日期、再選科目、再設時間），必要時調整順序，但不改變既有欄位所屬的分頁。
- [x] 區塊之間、標籤與輸入欄位之間的留白、對齊與分隔線須一致，避免不同分頁的版面風格落差過大。
- [x] 本項僅處理版面配置、分組與間距，色彩層次調整仍依「設定畫面彩色化」一節規劃，兩者可合併實作但需求分開記錄。

### 學生名單多欄卡片化

- [x] 「學生名單」分頁改為多欄卡片排列（依畫面寬度自動決定欄數），取代目前單欄逐列往下捲動的呈現方式。
- [x] 每位學生一張卡片，卡片內至少顯示座號與姓名，版面在窄螢幕下可退回單欄或雙欄，維持可讀性。
- [x] 卡片內「修改」「刪除」文字按鈕改為圖示按鈕（如鉛筆／垃圾桶），需保留可辨識的說明文字（如 `aria-label` 或 `title`），避免只靠圖示造成操作辨識困難。
- [x] 圖示按鈕的點擊區域大小需與原文字按鈕相當，避免誤觸或難以點選。
- [x] 學生人數變動、新增或刪除後，多欄卡片版面需正確重新排列，不得出現空卡片或錯位。
- [x] 此項僅調整學生名單的呈現排列與按鈕樣式，既有的修改、刪除、防呆邏輯與資料結構不變。

### 缺席名單卡片刪除圖示化

- [x] 「缺席名單」卡片上的「刪除」文字按鈕改為垃圾桶圖示按鈕，需保留可辨識的說明文字（如 `aria-label` 或 `title`）。
- [x] 2026-08-29 追加決議：手動缺席卡片上的「修改」按鈕整個移除，卡片僅保留垃圾桶刪除圖示；`editManualAbsent` 相關程式碼一併移除。
- [x] 圖示按鈕的點擊區域大小需與原文字按鈕相當，避免誤觸或難以點選。
- [x] 此項僅調整缺席名單卡片的刪除按鈕呈現，既有的刪除邏輯、缺席人數統計與名單資料結構不變。

### 新增單一學生欄位調整

- [x] 「新增單一學生」標題文字改為「新增學生」。
- [x] 新增區塊由單一「學生姓名」輸入框，改為「座號」與「姓名」兩個輸入框，一併送出新增。
- [x] 座號欄位的輸入與防呆規則（可留空、格式限制等）須與既有學生名單資料結構及編輯彈窗的座號欄位一致。
- [x] 新增時若座號重複或格式不符，需比照現有欄位的錯誤提示方式顯示訊息，不得直接新增錯誤資料。
- [x] 此項僅調整新增單一學生的欄位與標題文字，既有的姓名輸入、清單新增邏輯與資料結構其餘部分不變。

### 批次貼上學生姓名說明文字調整

- [x] 將批次新增學生的說明文字「每行一位，只需貼上姓名；程式會依順序從 1 開始自動編號。空白行會忽略，重複姓名會先提示。」改為「請依座號順序貼上學生姓名，座號會自動編上。」
- [x] 僅調整說明文字內容，既有的逐行解析、自動編號、空白行忽略、重複姓名提示等實際行為與規則不變。

### 移除語音聲音選擇

- [x] 移除「語音聲音」下拉選單及相關選擇介面，使用者不再手動指定語音提醒使用的聲音。
- [x] 語音提醒改為固定使用瀏覽器或系統預設的中文語音（或既有邏輯中的預設語音），語音提醒功能本身（開關、播報時機、內容）不變。
- [x] 移除下拉選單後，需一併處理既有本機儲存或離線使用檔中已儲存的聲音選擇設定，避免殘留設定造成錯誤或無效參照。
- [x] 確認移除後在未安裝額外語音包或語音清單為空的瀏覽器環境下，語音提醒仍可正常運作或明確降級（例如靜音但不報錯）。

### 離線使用檔功能正確性檢查

- [x] 檢查「下載離線使用檔」實際產出的 HTML 是否確實內嵌目前的學生名單、出缺席紀錄、考程設定、提醒與時鐘設定，內容需與下載當下畫面上的資料一致，不遺漏也不夾帶多餘資料。
- [x] 檢查下載出的 HTML 是否完全自包含，不依賴外部 CDN、字型、圖片、API 或任何網路資源，在斷網環境下開啟仍可正常顯示與運作。
- [x] 檢查在離線環境開啟該 HTML 後，修改名單、出缺席、考程、提醒與時鐘設定等操作是否能正確寫回該檔案自身的本機儲存，且重新整理或重新開啟後資料不遺失。
- [x] 檢查離線使用檔的修改與原本線上版本的資料是相互獨立（各自本機儲存），確認兩者不會互相覆蓋或混淆，行為需符合預期並讓使用者容易理解。
- [x] 檢查前面幾批已完成需求中要求「須內嵌於離線使用檔」或「不得依賴外部資源」的項目（如彩色化樣式、時鐘設定、PDF 解析防呆等）是否確實反映在目前的下載檔案中，逐一比對是否有遺漏。
- [x] 若發現下載檔案體積、內嵌內容或離線運作行為與需求不符，列出具體落差，作為後續修正依據。

### 時鐘樣式設定介面

- [x] 時鐘樣式只保留「指針式」與「數字式」，移除「翻頁式」選項及不再使用的相關介面。
- [x] 「樣式」取消下拉選單，改為同一行的「指針式／數字式」按鈕選擇，已選項目使用清楚的選取樣式。
- [x] 「大小」取消下拉選單，改為同一行的「特大／大／中／小」按鈕選擇。
- [x] 「位置」取消下拉選單，改為同一行的「靠上／置中」按鈕選擇。
- [x] 樣式、大小及位置按鈕在不同螢幕寬度下可以適度換行，但同一組選項須保持清楚且容易操作。
- [x] 修正數字式時鐘未套用位置設定的問題；「靠上／置中」必須同時對指針式與數字式有效。
- [x] 時鐘位置微調控制列及已儲存的水平、垂直偏移也須同時套用於數字式時鐘。
- [x] 從既有設定讀到已移除的「翻頁式」時，自動轉換為「數字式」，避免舊的本機設定或離線檔造成顯示異常。
- [x] 儲存時鐘設定後立即在主展示畫面生效，並寫入本機儲存及下載的離線 HTML。

### 上傳考程表匯入（PDF／Word）— 完整實作規格

> 2026-08-29 研究記錄：`extractPdfText()`「永遠解析不出考程」的根本原因已找到，並用實際學校考程表 PDF（`1141高初中期中考(1)時間科目表.pdf`）驗證過解法可行。同一份 PDF 的中繼資料顯示 `Creator/Producer: Microsoft® Word 2013`，也就是原始檔其實是 Word 文件——Word（.docx）的文字與表格結構是現成的 XML，不需要處理字型編碼與座標重建，難度遠低於 PDF，且能直接重用專案裡 [extractXlsxText](index.html) 已經在用的 ZIP＋`DecompressionStream('deflate-raw')` 解壓縮手法。**因此本節依「Word 優先、PDF 其後」排序**，兩者最終都餵進同一套年級篩選／逐筆確認／匯入流程（該流程本身的 UI 邏輯已經存在且正確，只是目前收到的候選資料是空的或假資料）。
>
> 使用者確認的流程：上傳檔案 → 本機解析出候選考程（含年級欄位）→ 先詢問使用者本班年級 → 只把該年級的考程送進可編輯確認表格 → 使用者逐筆勾選確認後才匯入。

#### 第 0 階段：上傳介面改版（PDF／Word 二擇一＋拖拉上傳）

- [ ] 上傳按鈕文字由「選擇 PDF」改為「選擇檔案」，`accept` 屬性同時接受 `.pdf` 與 `.docx`（`accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document"`）。
- [ ] 上傳區塊新增拖曳上傳：使用者可把檔案直接拖進「上傳 PDF」卡片區域放開即觸發解析，視覺上要有拖曳中的提示（例如邊框反白／變色），拖曳離開或放開後恢復原樣。
- [ ] 拖放與點選檔案選擇器兩種方式共用同一套「依副檔名判斷 PDF 或 Word」＋解析＋預覽的邏輯，不得各自維護一份重複程式碼。
- [ ] 副檔名或內容不符 PDF／Word 格式時，顯示清楚的錯誤訊息，不嘗試強行解析。

#### 第 1 階段：Word（.docx）解析引擎（優先實作）

> 2026-08-29 研究記錄：已用實際考程表 Word 檔（`1132一般年級暨高三期中考(1)時間科目表.docx`）完整解開並印出表格內容驗證，結構與規則如下，供實作直接參照。

- [ ] 比照 `extractXlsxText` 的做法解開 .docx 這個 ZIP 容器，取出 `word/document.xml`（純 UTF-8 XML，不需任何字型編碼轉換，這點比 PDF 簡單很多）。
- [ ] 解析 `<w:tbl>`／`<w:tr>`／`<w:tc>`／`<w:t>` 還原表格，每個儲存格文字＝該 `<w:tc>` 內所有 `<w:t>` 依序串接（同一格可能被 Word 拆成多個 `<w:t>` 執行片段，需全部串起來，不能只取第一個）。
- [ ] **實測驗證的真實表格版型**（16 列、5 欄，供對照）：
  - 第 0 列＝日期標題列：欄 0（`gridSpan=2`）是「日期」文字標籤，欄 2～4（依實際考程天數增減）各自是一天的日期，格式為中文全形「`3月17日(星期一)`」，**不是** PDF／既有 `normalizeDate()` 假設的 `2026/09/01` 數字格式，需要新增對應的日期正則（`(\d{1,2})月(\d{1,2})日`）並額外推算西元年（文件沒有年份資訊，可從檔名的民國學年度、或 `docProps/core.xml` 的建立時間、或既有「以程式目前年份為預設」的退回邏輯處理，務必處理跨年份/學期邊界，例如第二學期是 2、3 月但可能與程式執行當下年份不同）。
  - 第 0 列之後，每兩列成一組（「時間」列＋「科目」列），交替出現，一組＝一節考試：
    - 「時間」列：欄 1 文字固定是「時 間」，欄 2～4 是**該日該節**的時間範圍，格式「`8:05～8:55`」或「`8:05～8:45（40min）`」（同一節在不同日期，時間可能不同，且可能帶括號時長註記需先剝除）；分隔符號是全形波浪號「～」，現有 `parseScheduleText` 已有 `～`→`-` 的正規化可直接沿用。
    - 「科目」列：欄 1 文字固定是「科 目」，欄 2～4 是**該日該節**的科目，可能帶備註如「`英聽(考完自習) `」，需要用「取左括號前文字」或既有 `normalizeSubject` 的括號清除規則取出乾淨科目名稱；科目值也可能是「自習」，代表該節無考試，不應建立考程項目。
    - 欄 0 用 `vMerge`（`restart`／`continue`）跨列合併，標示這個時段區塊屬於「上　　午」或「下　　午」，僅供顯示參考，不影響時間欄本身已含的實際時刻。
  - 最後一列＝單一儲存格（`gridSpan`＝總欄數）的備註／試場規則說明文字，**不是考程資料**，需依「這一列只有 1 個 `<w:tc>` 且寬度等於整個表格欄數」判斷並排除，可另外顯示給使用者參考但不建立候選項目。
- [ ] 依「時間列＋科目列＋日期欄」組出候選清單，每筆＝ `{date, subject, start, end}`；科目為「自習」或空白者跳過，不建立候選。
- [ ] **年級偵測需同時支援「有年級欄」與「無年級欄」兩種真實版型**：已用第二份實測樣本（`1111一般年級暨初三期中考(2)時間表1111116.docx`，19 列 246 格）驗證存在明確的「年級」列——結構是：第 0 列日期標題，**同一天下有多個年級時，該日期儲存格的 `gridSpan` 等於當天的年級數**（例如 11/29 只有 2 個年級 `gridSpan=2`，11/30 有 5 個 `gridSpan=5`，12/1 有 6 個 `gridSpan=6`，需依此展開才能知道後續每一欄對應哪一天）；緊接著第 1 列是「年級」列（同樣欄 0 為 `gridSpan=2` 的「年級」標籤），從欄 1 起每欄一個年級文字（如「高二(理)」「高一」「初三」…），欄位順序與展開後的日期欄位一一對應。之後才是「時間」「科目」列，每欄分別對應各自年級當天的時段與科目。而第一份樣本（`1132一般年級暨高三期中考(1)時間科目表.docx`）完全沒有「年級」列，代表全表屬於同一班級。**解析邏輯需先偵測表格第 1 列（或第 0 列之後緊接著）欄 1 是否為「年級」字樣，有則依欄展開年級＋日期的對應關係；沒有則整份表格視為單一年級，交由使用者於匯入前指定。**
- [ ] **時間欄位需容錯處理真實文件常見的格式瑕疵**：實測發現同一份文件內時間字串會混用全形「～」與半形「-」，且偶爾因 Word 自動校正或跑版導致分隔符號遺失（例如「`9:009:50`」少了中間的「-」）。解析正則需能容忍缺少分隔符的情況（例如退回用「四位數字＋四位數字」的寬鬆規則再次嘗試），並在無法可靠切出起訖時間時，將該筆標記為「時間錯誤」低可信項目，交由使用者於確認表格中人工修正，不得放棄整筆或憑空猜測時間。
- [ ] 同一年級同一時段的科目文字可能帶科別／類組註記（如「歷史(理)」「歷史(文)」代表同年級不同分組的不同考科），應保留完整文字作為科目名稱，不要合併或截斷成同一科目，避免不同分組的考程被誤判成重複而互相覆蓋。
- [ ] 若文件內有多個表格，僅取「看起來像時間科目表」的表格（例如同時含有時間格式與科目文字的表格）建立候選清單，其餘表格或非表格說明文字忽略，不誤判為考程。

#### 第 2 階段：PDF 解析引擎重寫（Word 之後，難度較高）

- [ ] 用瀏覽器內建 `DecompressionStream('deflate')` 解開內容流與物件流（ObjStm）的 FlateDecode 壓縮（現有 `extractPdfText()` 完全沒做這步，是「永遠解析不出考程」的根本原因）。
- [ ] 解析每個內嵌字型各自的 `ToUnicode` CMap（`beginbfchar`／`beginbfrange`），把 CID 編碼的中文字正確還原成 Unicode；不同字型的 CMap 不可互相覆蓋混用。
- [ ] 同時處理一般西文字型（`WinAnsiEncoding`）用的括號字串 `(...)`，時間／日期數字常用這類非 CID 編碼字型。
- [ ] 追蹤 `Tm`／`Td`／`TD`／`T*` 等定位運算子，記錄每段文字在頁面上的座標，依座標重建表格的列與欄（PDF 沒有表格標籤，只有文字座標），才能正確配對日期、年級、節次與科目。
- [ ] 以上四步已用實測 PDF 個別驗證過技術可行，正式實作時整合成一條完整流程。

#### 第 3 階段：候選資料 → 年級篩選 → 匯入（銜接既有 UI）

- [ ] Word／PDF 兩種來源解析完成後，都輸出同一種候選資料格式（含 `grade` 欄位），餵進現有的 `importCandidates`／`renderImportPreview()`／`importParsedSchedules()` 流程，不需另外重做 UI。
- [ ] 年級選項依實際解析結果自動列出；若無法可靠辨識，允許使用者手動指定或逐筆修正（既有 UI 已支援手動指定，需確認資料源頭串接正確）。
- [ ] 解析完成後，在確認與匯入前明確要求使用者選擇本班所屬年級；選定後確認表格只顯示該年級的考程。
- [ ] 每筆解析結果保留可查看的年級欄位，避免不同年級在同一時間考不同科目時被錯誤混合。
- [ ] 解析時顯示目前階段進度（例如「解壓縮中」「解析表格」「整理考程」），讓使用者知道大檔案處理需要一點時間。
- [ ] 先以日期、時間格式、科目詞庫等規則標示缺少日期、科目不明、時間錯誤、重複科目及時段重疊等低可信項目，維持既有的「不得自動直接寫入考程，一律先進可編輯確認表格」原則。

#### 隱私與離線檔（已確認符合，實作新流程時延續）

- [x] 上傳的 PDF／Word 檔案僅作為快速新增考程的暫時來源；完成解析或離開頁面後，不把原始檔保存到 `localStorage`、設定 JSON 或其他持久資料。
- [x] 下載離線使用檔時，不內嵌上傳的原始檔、解析中間資料、完整辨識文字或其他年級的解析結果；即使檔案還留在選擇器或預覽中，下載前也要清除這些暫存內容。
- [ ] 解析全程（含 Word／PDF 兩種格式）都在瀏覽器內完成，不使用任何 AI API、雲端服務或使用者 API Key，也不上傳檔案內容。

#### 獨立、優先級較低：掃描 PDF 的 OCR

> 待評估單一 HTML 內嵌 OCR 辨識模型的檔案大小與瀏覽器相容性後再排入，不影響上述 Word／PDF 文字型解析的實作順序。

- [ ] 掃描型或無法取得有效文字的 PDF，自動將頁面轉為圖片後使用瀏覽器端繁體中文 OCR 辨識。
- [ ] OCR 元件、必要的繁體中文辨識資料及 PDF 頁面轉圖功能須能隨離線 HTML 一起使用，不依賴執行當下的外部網路資源。
- [ ] 若完整內建 OCR 造成離線 HTML 明顯增大，需在下載前顯示預估檔案大小，但仍維持單一檔案離線使用的目標。

### 考前提醒欄位排列

- [x] 將「語音提醒」核取選項移到「第一次提醒（分鐘）」標籤的正後方，兩者在同一行顯示。
- [x] 第一次提醒的分鐘輸入框排列在標籤列下方並使用可用寬度，不要讓語音提醒出現在輸入框右側。
- [x] 在較窄螢幕下，標籤與語音選項應優先維持同一行；空間確實不足時才整組自然換行，避免語音選項與所屬提醒階段分離。

### 已結束考程未自動隱藏

- [x] 修正今日考程在結束時間已過後仍留在清單中的問題。
- [x] 隱藏條件必須同時比較可校正的程式日期與時間：考程日期等於程式日期，且目前時間大於或等於該科結束時間時，立即從今日考程清單隱藏。
- [x] 判定時統一使用分鐘或完整時間戳，避免字串比較、上午／下午格式或秒數造成錯誤。
- [x] 程式時間經快速校時、與設備時間同步、跨過午夜或頁面重新整理後，都必須重新計算已結束狀態。
- [x] 使用者切換考試日分頁後立即重新判定；未來日期仍顯示全部考程，過去日期不再提供分頁。
- [x] 考程結束的當下不需重新整理頁面，清單、主要考試資訊、進行中標記及時鐘色條必須同步更新。
- [x] 加入邊界驗證：例如考程為 `10:10－10:35`，程式時間在 `10:34:59` 仍顯示，從 `10:35:00` 起自動隱藏。
- [x] 當所選的今日考試日已無未結束考程時，顯示「今日考程已全部結束」，不得繼續顯示最後一科。

### 考試結束後切換自習模式

- [x] 每科考程結束後先保留「考試結束」狀態一段緩衝時間，預設為 `5` 分鐘，讓監考老師完成收卷與指示。
- [x] 緩衝時間結束且當天仍有下一科考試時，自動切換為自習模式，不需重新整理頁面。
- [x] 自習模式的主要訊息顯示「請安靜自習」。
- [x] 自習模式顯示下一科提示，格式為「下一科考試為 OO 科（將於 OO 分後開始）」；第二個 `OO` 依校正後的程式時間動態倒數。
- [x] 倒數分鐘採一致的整數顯示規則，尚未到達開始時間前不得提前顯示 `0 分`；跨分鐘時自動更新。
- [x] 若考試結束後已沒有下一科，緩衝時間結束後顯示「今日考程已全部結束」，不進入下一科自習提示。
- [x] 狀態優先順序須明確：考試進行中 → 考試結束緩衝 → 安靜自習／下一科提示 → 各階段考前提醒 → 下一科考試進行中。
- [x] 當下一科進入第一次提醒、強提醒或最後提醒門檻時，考前提醒內容應取代一般自習提示；提醒結束後依實際狀態繼續更新。
- [x] 在「設定」的提醒與時間頁面新增「考試結束後幾分鐘轉為自習」數字設定，預設 `5` 分鐘並限制為大於或等於 `0` 的整數。
- [x] 在同一設定區新增「自習模式提醒訊息」文字框，預設為「請安靜自習」，允許使用者修改。
- [x] 自習切換分鐘與提醒訊息須自動儲存在 `localStorage`，並納入設定 JSON 及下載的離線 HTML。
- [x] 快速校時、與設備時間同步、跨日及重新整理後，必須依設定重新判斷目前應處於緩衝、自習、考前提醒或考試狀態。
- [x] 加入流程驗證：例如第一科 `10:10－10:35`、下一科 `11:10` 開始、緩衝設定 `5` 分鐘，則 `10:35－10:39:59` 顯示考試結束，從 `10:40` 起顯示安靜自習及下一科倒數。

### 語音聲音選擇（已於 2026-08-29 決議移除，本節作廢）

> 本節原為新增語音聲音選擇功能的規格，程式先前已依此實作；2026-08-29 使用者決議改為固定自動挑選中文語音、移除手動選擇介面（見上方「移除語音聲音選擇」節），與本節需求相反。本節保留作為歷史紀錄，不再視為待辦，請勿依本節重新實作語音選擇下拉選單。

- [ ] 在「設定」的提醒與時間頁面新增語音聲音選擇，透過瀏覽器 `speechSynthesis.getVoices()` 取得目前設備可用的語音。
- [ ] 語音選單優先列出繁體中文與其他中文語音，並顯示使用者可辨識的語音名稱及語系。
- [ ] 若設備沒有中文語音，仍列出可用語音並顯示提示，讓使用者知道發音效果可能不同。
- [ ] 新增語速設定，提供容易理解的選項或滑桿，並設定適合考場廣播的預設語速。
- [ ] 新增音量設定，允許使用者在不改變系統音量的情況下調整程式語音音量。
- [ ] 「測試語音」必須使用目前選定的聲音、語速與音量播放，方便老師在教室現場確認效果。
- [ ] 選定的語音以穩定識別資訊儲存；若離線檔換到另一台電腦後找不到同一語音，自動改用該設備的預設中文語音。
- [ ] 若同一設備連預設中文語音也不存在，退回瀏覽器預設語音，不得使提醒功能發生錯誤或中斷其他計時功能。
- [ ] 語音、語速及音量設定須儲存在 `localStorage`，納入設定 JSON 與下載的離線 HTML。
- [ ] 因部分瀏覽器會延後載入語音清單，程式須監聽 `voiceschanged` 並更新選項，避免設定頁第一次開啟時清單為空。

## v2.2.0 修改（已完成）

### 考試進行中的時間資訊

- [x] 考試進行中不再顯示「剩餘 22:48」等即時剩餘時間倒數，避免增加學生緊張感。
- [x] 考試進行中仍保留「考試進行中」、目前科目及考試起訖時間。
- [x] 放大主要考試資訊中的起迄時間，使其在 85 吋螢幕上更容易辨識。
- [x] 稍微增加考試科目與起迄時間之間的垂直行距，讓兩項資訊的層次更清楚。
- [x] 起迄時間後方加上考試總時長，格式為「10:10－10:35（共 25 分）」；分鐘數由開始與結束時間自動計算。
- [x] 移除剩餘時間後重新調整資訊區的垂直間距，避免留下過大的空白或版面失衡。
- [x] 考前倒數提醒仍依設定運作；本項只移除考試開始後的數字剩餘時間顯示。

### 指針時鐘的考試進度色條

- [x] 倒數色條改為依時鐘方向順時針前進，不再使用方向與時鐘運行方向不一致的縮減方式。
- [x] 60 分鐘以內的考試依真實分鐘刻度標示：開始時間顯示實心圓點，結束時間顯示清楚的終點旗標或菱形標記。
- [x] 考試開始後，實心圓點沿著外圈由開始刻度順時針移向結束標記；已經走過的色條消失，只保留目前位置到終點之間的色條。
- [x] 色條僅提供視覺化時間進度，不顯示剩餘分鐘或秒數。
- [x] 剛好 60 分鐘的考試，開始點與結束點可位於同一刻度；以不同造型區分實心移動圓點與固定終點標記，圓點繞行一圈後抵達終點。
- [x] 超過 60 分鐘的考試採「每小時一圈」方式，外圈只呈現目前時段的進度，避免多圈或多層圓環造成畫面複雜。
- [x] 超過 60 分鐘時，在時鐘旁以低干擾的小型文字顯示「第 1／2 時段」等時段資訊，不顯示剩餘時間。
- [x] 每完成一個小時，移動圓點回到下一時段的起點繼續前進；最後一個時段的終點必須落在實際結束分鐘的刻度。
- [x] 例如 10:10～11:40：第一時段為 10:10～11:10，圓點走完整圈；第二時段為 11:10～11:40，圓點由 10 分刻度移至 40 分刻度。

### 時鐘操作、校時與配色

- [x] 時鐘位置微調控制鍵維持貼齊時鐘區塊下邊緣，但由最左側向右移至區塊水平約 `1/9` 的位置，降低大螢幕邊緣裁切控制鍵的風險。
- [x] 時鐘區塊右下方新增「設定時間」按鈕，讓使用者不必進入後台即可快速校正程式時鐘。
- [x] 按下「設定時間」後開啟小型校時視窗，可直接修改目前的時、分、秒，並立即預覽校正後的指針位置。
- [x] 校時視窗新增「秒針歸零」按鈕；按下後只將秒數設為 `00`，保留當下設定的時與分。
- [x] 「秒針歸零」在任何秒數下都不得四捨五入或使分鐘進位，例如 `10:35:59` 歸零後必須成為 `10:35:00`，不得變成 `10:36:00`。
- [x] 校時完成後沿用既有的程式時間偏移與本機儲存機制，重新整理頁面及離線使用時仍能保留設定。
- [x] 重新調整指針時鐘整體配色，提高時針、分針、秒針、刻度、外圈進度色條及終點標記的辨識度，同時避免純白高亮或過度飽和造成刺眼。
- [x] 配色調整須維持黑色背景，並以 85 吋螢幕遠距觀看及長時間監考的舒適度為驗收基準。

### 展示畫面的考程快速管理

- [x] 在展示畫面的每一筆既有考程旁新增「修改」與「刪除」按鈕，讓使用者不必進入後台即可管理考程。
- [x] 按下「修改」後開啟簡易修改視窗，可調整該筆考程的日期、科目、開始時間與結束時間。
- [x] 修改視窗沿用新增考程的時間操作方式、快捷時長及科目防呆，並在儲存前檢查結束時間與考程重疊。
- [x] 修改儲存後立即更新今日考程、主要考試資訊、時鐘進度色條及相關提醒，並同步寫入本機儲存。
- [x] 按下「刪除」時先顯示包含日期、科目及時間的確認訊息，確認後才刪除，避免誤觸。
- [x] 刪除後立即更新畫面、提醒排程與本機儲存；若刪除的是目前正在進行的考試，也必須立即重新判定展示狀態。
- [x] 「修改」與「刪除」按鈕須維持適合大螢幕觸控的尺寸，但不得遮擋或壓縮考程文字。

### 今日考程的動態顯示

- [x] 今日考程區自動隱藏結束時間已早於目前程式時間的考程，只保留正在進行及尚未開始的考程。
- [x] 隱藏判定必須使用可校正的「程式時間」，不可直接固定使用裝置系統時間。
- [x] 正在進行的考程須使用與未開始考程不同的卡片底色、邊框或「進行中」標籤清楚區別，同時維持黑色背景下舒適、不刺眼的效果。
- [x] 考程從「尚未開始」進入「進行中」，以及從「進行中」變成「已結束」時，畫面須自動即時更新，不必重新整理。
- [x] 切換查看未來日期時顯示該日全部考程，不得以今天的目前時間隱藏未來考程。
- [x] 當今天所有考程皆已結束時，今日考程區顯示簡潔的「今日考程已全部結束」訊息，不留下空白區塊。

### 無學生名單時的出缺席手動操作

- [x] 將「應到」與「實到」的數字顯示改為可直接輸入的數字框，並在旁邊提供增加、減少的微調按鈕。
- [x] 應到與實到只接受大於或等於 `0` 的整數；實到不得大於應到，直接輸入與按鈕微調都必須套用相同防呆。
- [x] 「缺席」人數固定由「應到－實到」即時計算，不提供獨立輸入或調整，避免三個數字互相矛盾。
- [x] 缺席數字使用與應到、實到不同的高辨識警示樣式，但避免過度刺眼，並保持黑色背景上的遠距可讀性。
- [x] 缺席名單區新增「新增」按鈕，按下後開啟簡易彈窗，可直接輸入缺席者的座號與姓名。
- [x] 未匯入學生名單時也能新增、修改及刪除手動輸入的缺席者，並以既有的獨立姓名卡片呈現。
- [x] 手動缺席名單須檢查空白資料及重複座號；若沒有座號，仍允許只輸入姓名。
- [x] 手動新增或刪除缺席者不自行改變應到與實到數字；缺席人數仍以應到減實到為唯一依據。
- [x] 若手動缺席名單筆數與計算出的缺席人數不同，顯示清楚但不干擾考場的提示，讓使用者自行校正資料。
- [x] 應到、實到及手動缺席名單的修改皆立即更新畫面並寫入本機儲存，支援未匯入名單及離線使用情境。

### 操作名稱與入口整理

- [x] 移除原本舊位置的「調整時鐘時間」按鈕，避免與時鐘區右下角新規劃的快速「設定時間」功能重複。
- [x] 將所有「後台設定」文字統一改為「設定」，包含按鈕、頁面標題與返回後的相關提示。

## 本次重設計（已完成）

- [x] 85 吋考場展示版面：左上固定指針時鐘，右上主要考試資訊。
- [x] 指針時鐘外圍提供彩色粗線倒數環；考試中顯示剩餘比例，自習時顯示距下一科倒數。
- [x] 上方主資訊區占畫面約三分之二，避免被監考老師遮擋。
- [x] 下方顯示今日考程、應到／實到／缺席人數與缺席名單。
- [x] 考試之間自動顯示「安靜自習」，不需建立自習考程。
- [x] 提供考前 10、5、1 分鐘分級提醒，門檻與準備文字可調整。
- [x] 科目依國文類、英文類、數學、社會科、自然科排序，不顯示類組。
- [x] 內建 25、40、50、60、70、80、95 分鐘快捷時長與自訂時間。
- [x] 已排入考程的科目顯示排定資訊、停用按鈕且不得重複新增。
- [x] 考程時間重疊、缺漏與結束時間錯誤防呆。
- [x] 老師設定後台：考程、學生名單、出缺席、提醒與模擬時間、備份與離線。
- [x] 可直接貼上「座號 姓名」或試算表兩欄學生名單並檢查重複座號。
- [x] 所有現場修改自動儲存到依設定 ID 區隔的 `localStorage`。
- [x] 可下載內嵌目前設定、零外部資源且保留全部修改功能的單一離線 HTML。
- [x] 支援設定 JSON 匯入／匯出與回復 HTML 內建設定。

## 待實際考場驗收

- [ ] 以教室 85 吋螢幕實測最後一排可讀性與監考老師遮擋範圍。
- [ ] 在實際離線大屏電腦以 `file://` 開啟下載檔，確認該瀏覽器允許本機儲存與下載更新檔。

## v2.0.0 修改（已完成）

### 顯示與配色

- [x] 為節省大螢幕用電，所有頁面與區塊背景一律改為純黑色。
- [x] 文字依所在區塊自動調整顏色、亮度與對比，確保在黑色背景及 85 吋螢幕上清楚可讀。
- [x] 缺席資訊卡改用醒目的高警示配色，讓監考老師能立即辨識。

### 時鐘與時間調整

- [x] 指針式時鐘區不再顯示數字時鐘及「現在時間」文字。
- [x] 在數字時鐘旁增加「調整時間」按鈕，方便快速進入時間調整功能。

### 語音提醒

- [x] 在每一個考前提醒階段旁增加獨立的「語音提醒」選取項目，讓各階段可分別開啟或關閉語音。
- [x] 所有考前提醒階段的語音選項預設為未選取，避免進入頁面後自行播放聲音。
- [x] 語音選項的設定需儲存在 `localStorage`，並包含在設定備份及下載的離線 HTML 中。

### 後台與考試科目

- [x] 將「老師設定後台」更名為「後台設定」，並同步修改所有相關按鈕、標題與提示文字。
- [x] 增加考試科目刪除功能；刪除前需提示確認，並妥善處理已排入考程的科目，避免留下無效考程。

### 上傳考程表與自動解析

- [x] 在後台設定增加「上傳考程表」功能，支援學校常用的 PDF、圖片、Excel 與 CSV 檔案。
- [x] 自動辨識考試日期、開始時間、結束時間、考試時長與科目名稱。
- [x] 將「國語文／英語文」等名稱轉換為系統標準科目名稱，忽略文理類組標記，並依國文、英文、數學、社會科、自然科的順序整理。
- [x] 解析後先顯示可編輯的預覽結果，標示無法辨識或可信度不足的欄位；必須經老師確認後才正式匯入考程。
- [x] 匯入前檢查重複科目、時間重疊、日期或時間缺漏及結束時間錯誤，不直接覆寫既有考程。
- [x] 若檔案包含多個年級、班級或類組，提供逐筆選擇要匯入之考程的操作介面。
- [x] 評估離線版的解析能力與檔案大小；無法完全離線解析的格式需清楚提示限制，且不影響手動新增考程。

### 學生名單管理

- [x] 名單匯入方式改為依序貼上學生姓名，由程式依貼上順序自動編號。
- [x] 保留貼上內容的原始順序，並處理空白行及重複姓名的提示。
- [x] 增加單筆新增學生功能，新增後自動編排座號。
- [x] 增加修改學生姓名功能。
- [x] 增加刪除學生功能；刪除前需提示確認，刪除後自動重新編號並同步更新出缺席資料。

## v2.1.0 需求與驗收規格

> v2.1.0 已完成本節的介面、時鐘、提醒、科目管理、日期分頁與離線儲存功能。下列勾選框保留為逐項實際考場驗收清單。
>
> 2026-08-29 稽核：對照目前程式碼逐項核對本節，多數項目其實已經實作（先前只是沒回頭補勾），一併補記為完成；少數項目已被後續決議取代或作廢，加註說明；仍有兩項是貨真價實還沒解決的問題，保留未勾選並附上目前的關聯線索。

### 黑色背景與文字色彩

- [x] 保留黑色背景，但降低大面積純白文字造成的刺眼感（`--text:#d8e0e8` 柔和淺灰，非純白）。
- [x] 依資訊類型調整文字色彩：一般內容柔和淺灰、次要說明較暗灰色（`--muted`）；進行中、即將開始、警告與錯誤訊息使用差異色（`--green`／`--yellow`／`--orange`／`--red`）。
- [ ] 確保所有文字在 85 吋螢幕與遠距觀看時仍具有足夠對比與清晰度（需實機驗收，見「待實際考場驗收」節）。

### 考前語音提醒版面

- [x] 本節全部項目已於「考前提醒欄位排列」節（見上方「下一批待修改需求」）逐項核對完成：語音提醒與提醒分鐘同一行、三階段用分隔線區隔、各自獨立提醒訊息文字框、進入對應倒數階段時分別顯示、設定值存 `localStorage` 並納入備份與離線 HTML、保留合理預設訊息。不重複列出，請參照該節。

### 後台返回按鈕

- [x] 「返回展示」已改為「返回」。
- [x] 後台頁籤已更名為「離線使用」。
- [x] 頁籤內標題、說明與按鈕文字已統一使用「離線使用」相關名稱。
- [x] 下載按鈕文字已改為「下載離線使用檔」。
- [x] 下載檔名為 `段考小幫手_離線使用_YYYY-MM-DD.html`，可清楚辨識為離線使用檔案。

### 考程檔案解析訊息版面

> 2026-08-29 訂正：本節與上方「上傳考程表匯入（PDF／Word）— 完整實作規格」高度重疊，且部分要求已被後續決議取代。PDF 解析準確度、表格座標重建、可信度標示、可編輯預覽等技術性要求，一律以上方那節的規格與研究記錄為準，不在本節重複維護；本節僅保留仍需個別確認的版面／文字項目。

- [x] 「上傳考程表」標題已改為「上傳 PDF」（後續依 Word 匯入功能上線時，將依上方規格再改為「選擇檔案」，屆時本項需同步更新）。
- [x] 考程上傳目前僅支援 PDF（`accept=".pdf,application/pdf"`）——**此項會隨上方「第 0 階段：上傳介面改版」被有意變更為同時支援 PDF／Word，屆時本項描述需要一併修訂，不是永久規格**。
- [x] 「可直接解析 CSV、TXT、TSV 及文字型 PDF……」這段舊說明文字目前已不存在於介面上。
- [ ] 程式碼裡仍留有從未被呼叫到的 `extractImageText()`、`extractXlsxText()` 死程式碼（上傳入口早已限定只接受 PDF，這兩個函式沒有任何呼叫點）。上傳介面改版（第 0 階段）或 Word 解析引擎（第 1 階段）動工時，應一併評估：`extractXlsxText` 的 ZIP／`deflate-raw` 解壓縮手法會被 Word 解析重用（等於「重寫並保留」），但 `extractImageText`（用 `TextDetector` 讀圖片文字）目前完全用不到，若確定不需要圖片格式匯入應直接刪除，不要放著當死碼。
- [ ] 使用者以拖放等方式提供非 PDF／Word 檔案時，顯示簡短且明確的提示，不進行解析（對應上方第 0 階段的拖放上傳需求）。
- [ ] PDF 辨識準確度、表格座標重建、OCR、日期／時間格式強化、科目正規化、可信度標示、可編輯預覽等技術需求，全部以上方「上傳考程表匯入（PDF／Word）」節的規格為準，此處不重複。
- [ ] 分別驗證尚未選檔、解析中、解析成功、解析失敗四種狀態的版面（含拖放上傳的視覺回饋），待第 0～2 階段實作完成後一併驗收。

### 已選定科目與科目管理

- [x] 欄位名稱已是「已選定科目」。
- [x] 點選上方考試科目會自動帶入「已選定科目」輸入框（`selectSubject()`）。
- [x] 輸入框旁已有「新增」「修改」「刪除」三個按鈕。
- [x] 「新增」會檢查空白與重複名稱（`addSubject()`）。
- [x] 「修改」會同步更新已使用該科目的考程資料（`renameSubject()` 會同步更新 `config.schedules`）。
- [x] 「刪除」前會提示確認，且若科目已排入考程會清楚提示並阻擋刪除（`deleteSubject()`）。
- [x] 新增、修改、刪除後維持科目排序（`subjectRank`）與已排定科目防重複規則。

### 缺席名單卡片

- [x] 缺席名單每位學生已是獨立卡片顯示（`.absent-person`），非合併成一行文字。
- [x] 每張卡片顯示座號與姓名，並用 `flex-wrap` 自動換行避免超出展示區。

### 今日考程的日期分頁與顯示邏輯

- [x] 「今日考程」已依考試日期建立第 1 天、第 2 天…分頁，分頁上顯示實際月日（`day-tab`）。
- [x] 系統日期在第一個考試日時，可切換查看該日與後續考試日；在最後一個考試日時只顯示當天，不提供過去考試日分頁（`renderToday()` 的 `available=dates.filter(x=>x>=actual.key)` 邏輯）。
- [x] 每次重新整理或跨日後自動回到當天考程，不保留過期日期的選取狀態（`if(!selectedScheduleDay||!available.includes(selectedScheduleDay))` 重設邏輯）。
- [x] 切換查看未來考程只改變列表內容，主畫面目前考試／倒數／考前提醒仍以實際目前時間為準（`updateScreen()` 與 `renderToday()` 各自獨立運作）。
- [x] 若今天不在考試期間，顯示「今天沒有考程」與下一個考試日資訊（`nextExamDay()`），不會誤把未來考程當成今日進行中。

### 主畫面操作按鈕與卡片框線

> 2026-08-29 提醒：本節第一項與今天測試手機版面時發現的問題是同一件事——`.bottom-actions` 為 `position:fixed`，在內容較長需捲動的頁面（尤其行動裝置直式版面）會浮貼在畫面右下角，捲動到底部附近時可能與卡片內容重疊。今天只把行動裝置的 `padding-bottom` 從 13vh 微調到 16vh 做初步緩解，未徹底解決。
>
> 2026-08-29 v2.4.3 已徹底解決：行動裝置（≤900px）不再用 `position:fixed`，改為 `position:static` 排在主內容之後（因此任何捲動位置都不可能壓到卡片，`padding-bottom` 也從 16vh 降回 1.25vh）；桌面維持 fixed，但改用執行期量測的 `--toolbar-h` 讓 `.screen` 的 `padding-bottom` 取 `max(8vh, 工具列高度+16px)`，矮視窗（例如 600px 高，8vh 只有 48px）不會再被工具列蓋到。

- [x] 修正右下方時間及操作按鈕壓住卡片內容或底部框線的問題（含桌面與行動裝置版面）。
- [x] 在主內容底部保留足夠的安全間距，固定操作列不得遮住卡片內容或邊框。桌面 `padding-bottom:max(8vh,calc(var(--toolbar-h) + 16px))`，`--toolbar-h` 由 `trackToolbarHeight()` 以 `ResizeObserver` 實際量測工具列高度寫入。
- [x] 操作列在畫面寬度不足時已用 `flex-wrap` 自動換行（行動裝置版面 `.bottom-actions{flex-wrap:wrap}`）。
- [x] 檢查缺席警示粗框啟用與未啟用兩種狀態，按鈕均不得與框線干涉：以應到 30／實到 27 開啟 `.absence-card.alert` 3px 粗框後實測，工具列與五張卡片的矩形皆無交集。
- [x] 以一般電腦畫面（1366×768，卡片下緣與工具列間距 33px）、矮視窗（1366×600，間距 6px、無捲動）及窄畫面（390×844，工具列已在流內、與卡片零重疊）三種尺寸驗證操作列版面；85 吋仍待實機驗收（見「待實際考場驗收」節）。

### 調整時鐘時間

- [x] 主畫面按鈕文字已是「設定時間」（對應規格「調整時鐘時間」的功能，文字略有出入但功能一致）。
- [x] 按下後顯示小型時間調整視窗，不進入完整後台設定（`openClockTimeDialog()`／`clock-time-modal`）。
- [x] 小視窗提供時、分、秒快速調整欄位，預設顯示程式目前時間。
- [x] 套用後只調整程式顯示時間，不變更電腦系統時間；提供「與設備時間相同」可恢復（`syncDeviceTime()`）。
- [x] 調整值（`config.settings.timeOffset`）會透過 `saveConfig()` 存入 `localStorage`，並納入設定備份與離線 HTML。

### 時鐘樣式設定

> 2026-08-29 訂正：「翻頁式」樣式已於後續決議移除（見「時鐘樣式設定介面」節），只保留指針式／數字式，本節該項描述已過時。可調整範圍限制（避免時鐘移出顯示區）已於 v2.4.3 修正，見本檔案最上方「時鐘微調範圍應依時鐘實際大小動態調整」節。

- [x] 後台「提醒與時間」頁籤右方已有「時鐘樣式」設定區。
- [x] 樣式改為指針式／數字式二選一（原規格的「翻頁式」已作廢，不再適用）。
- [x] 已提供時鐘大小選項：特大、大、中、小。
- [x] 已提供時鐘垂直位置選項：靠上、置中。
- [x] 時鐘區已有上下左右四個方向按鈕可微調位置（`clock-nudge`）。
- [x] 每次按鈕只移動固定小幅距離並立即反映在主畫面（`moveClock(dx,dy)`）。
- [x] 限制可調整範圍避免時鐘移出顯示區（v2.4.3 以 `clockNudgeLimits()`／`clampClockOffset()` 依實際尺寸動態計算，詳見本檔案最上方「時鐘微調範圍應依時鐘實際大小動態調整」節）。
- [x] 已提供「恢復預設位置」按鈕（`resetClockPosition()`）。
- [x] 微調位置會自動存入 `localStorage`（`saveConfig()`），重新載入後維持設定。
- [x] 時鐘位移設定已納入設定 JSON 備份與離線 HTML（皆為 `config.settings` 的一部分）。
- [x] 樣式、大小、位置改變會立即套用（`applyClockSettings()`），等同即時預覽。
- [x] 數字式時鐘也能清楚呈現時間（今天已重新調校字體大小，隨容器實際尺寸縮放）。

### 指針時鐘外框與倒數色條

- [x] 已移除額外的粗倒數圈，只保留單一主要外框（`ring-track`／`ring-progress`）。
- [x] 尚未倒數時外框維持中性色彩（`ring.style.stroke=counting?ringColor:'transparent'`）。
- [x] 開始倒數後外框依剩餘時間切換對應階段色彩並逐步縮減（`renderExamProgress()`）。
- [x] 考試結束或離開倒數狀態後外框恢復正常樣式。
- [x] 指針時鐘的時針與分針已統一為藍色（`.hour-hand{background:#4cbcf3}`／`.minute-hand{background:#83d5fa}`），並以長度、粗細差異區分，黑底下清晰可辨。

## v2.1.0 實作結果

- [x] 柔和高對比文字配色、獨立缺席學生卡片與底部安全間距。
- [x] 三階段提醒分隔、各自訊息文字框、同行語音選項與階段訊息顯示。
- [x] 已選定科目的新增、修改、刪除與已排定科目防呆。
- [x] 今日及未來考試日分頁，跨日自動隱藏過去考試日。
- [x] 獨立的時、分、秒「調整時鐘時間」小視窗。
- [x] 指針式、數字式、翻頁式及大小、靠上／置中設定。
- [x] 時鐘四向微調、邊界限制、恢復預設及自動儲存。
- [x] 移除額外粗圈，主要外框只在倒數時變色縮減；時針與分針改為藍色。
- [x] 上傳入口限 PDF、改善文字流與編碼字串擷取、提供可編輯預覽及匯入防呆。
- [x] 「返回」、「離線使用」及「下載離線使用檔」等介面名稱更新。
- [ ] 進階掃描 PDF 中文 OCR、文字座標表格重建與實際學校 PDF 完整辨識率驗收；單一離線 HTML 不內嵌大型 OCR 模型。

## v2.1.1 修正（已完成）

- [x] 將時鐘上、下、左、右與恢復預設位置按鈕移到主畫面時鐘旁。
- [x] 操作方向按鈕時可直接看到時鐘即時移動，位移後繼續自動儲存。
- [x] 後台不再重複顯示方向控制器。
